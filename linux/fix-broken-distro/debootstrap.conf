# Debootstrap configuration for custom Debian-based distro
# WARNING: This configuration contains deliberate errors for testing purposes

# Architecture configuration - BROKEN: wrong architecture
ARCH="i368"

# Suite/Release
SUITE="bookworm"

# Mirror
MIRROR="http://deb.debian.org/debian"

# Target directory
TARGET_DIR="/mnt/custom-distro"

# Include packages - BROKEN: missing critical kernel package
INCLUDE_PACKAGES="systemd,udev,network-manager,grub-pc,sudo,vim,openssh-server"

# Exclude packages
EXCLUDE_PACKAGES="vim-tiny,nano"
